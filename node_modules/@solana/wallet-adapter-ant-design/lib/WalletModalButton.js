var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { Button } from 'antd';
import React, { useCallback } from 'react';
import { useWalletModal } from './useWalletModal';
export const WalletModalButton = (_a) => {
    var { children = 'Select Wallet', type = 'primary', size = 'large', onClick } = _a, props = __rest(_a, ["children", "type", "size", "onClick"]);
    const { setVisible } = useWalletModal();
    const handleClick = useCallback((event) => {
        if (onClick)
            onClick(event);
        if (!event.defaultPrevented)
            setVisible(true);
    }, [onClick, setVisible]);
    return (React.createElement(Button, Object.assign({ onClick: handleClick, type: type, size: size }, props), children));
};
//# sourceMappingURL=WalletModalButton.js.map